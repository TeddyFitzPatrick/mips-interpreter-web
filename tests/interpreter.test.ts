import { describe, it, expect } from "vitest";
import { registers, registerNames, runProgram } from "../app/interpreter";

// describe('VALIDATE ADD INSTRUCTION', () => {
//   it('  ', () => {
//     const program = `

//     `;

//     runProgram(program)

//     expect(registers['$t0']).toBe(5)
//     expect(registers['$t1']).toBe(10)
//     expect(registers['$t2']).toBe(15)
//   })
// })